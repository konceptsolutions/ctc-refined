import { SalesInvoice } from "@/components/sales/SalesInvoice";

/** Transfer Out — same form layout as Sales Invoice. */
export const TransferOut = () => <SalesInvoice documentKind="transfer-out" />;
